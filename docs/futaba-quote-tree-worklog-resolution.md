# 引用ツリー作業ログ（解決結果の一元化）

## 2026-09-05

- 重複なしツリー（`dedupTree2`）で、子レスの本文引用がリンク化されない再現例を確認。
- 引用解決結果を `futabaQuoteResolutions` に保持し、ツリー構築・逆引き・本文描画が同じ結果を参照するよう変更。
- 描画時に件数と末尾番号だけでキャッシュ判定せず、レス番号・引用深度・引用本文から署名を作成して再同期時の古い解決結果を防止。
- プレビューとWebView再同期では引用解決結果もリセットする。
- 再現例（1432660797 → 1432661031）の引用解決テストを追加。

## 検証

- `node --check`（`thread.js` / `futaba-quotes.js`）：成功。
- `node --test tests/*.test.cjs`：12件成功。
- `git diff --check`：終了コード0。
- Debugビルド（通常出力）：警告0・エラー0。
- UI上の実スレ表示確認：ユーザー確認待ち。
- 追加調査で、表示側が `rawHtml` の有無に依存して原文へ戻る経路を確認。引用リンク・後続本文とも `OriginalText` を使うよう修正。
